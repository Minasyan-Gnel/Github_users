const Repos = () => {
    const rootPage = document.getElementById("root-page");
    const html = `<div id="repos-wrapper">
        repos wrapper
    </div>`;
    rootPage.innerHTML = html;
}

export default Repos;